# Cleanlist homepage redesign preview

- `/`          option-c, the checkpoint. Warm ink and cream.
- `/option-d/` option-d, the designer's cool palette (cobalt, slate, teal on near-white).

Built from `tasks/home-redesign-aug2026/v5/option-{c,d}/` via `node _deploy.mjs --verify`.
